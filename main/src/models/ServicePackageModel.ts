declare type ServicePackageModel = {
    id: number,
    title: string,
    description: string,
    cost: number,
    photos: string[],
    rating: number,
    duration: number
}

export default ServicePackageModel;